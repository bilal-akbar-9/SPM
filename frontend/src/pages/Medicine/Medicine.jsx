import React from 'react'

const Medicine = () => {
  return (

    //create a search bar and a dropdown for "medicine search and filter" module and then update their respective states and keep things dynamic
    //use a dialog and create a plus button to add a new medicine
    <div>Medicine</div>

    //fetch for the meds and then display them in medicineTable

  )
}

export default Medicine